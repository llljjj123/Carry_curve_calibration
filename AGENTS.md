Context: Read `session_log.md`. We have done most of the parts 

Goal: Write a `summary_for_AI.md` so that someone else could feed this to his/her AI and the AI would know about the project.

Todo: Read `session_log.md` and codes in all 5 folders, come up with a `summary_for_AI.md`, output this into root directory. Don't change anything during the process.

I am also working on `README.md` under root directory `Carry_curve_calibration`, you don't need to read or summarize anything in there.