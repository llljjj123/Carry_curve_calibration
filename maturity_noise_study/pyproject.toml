[tool.pytest.ini_options]
pythonpath = [".", "../Demo", "../im_2factor_ou_carry/src", "../carry_put_pricing/src"]
testpaths = ["tests"]
addopts = "-ra"

[tool.ruff]
line-length = 100
target-version = "py310"

